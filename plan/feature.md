# Feature: Icon, docs site, and integrated docs CI

## Source
Three pending requests in `$DOC_ROOT/Tharga/Requests.md`:
- "Move PackageIconUrl to thargelion.net/assets" → Tharga.Test (line 1652)
- "Documentation sites under tharga.net" → Tharga.Test (line 1776)
- "CI: Publish docs after release succeeds" — applied from day one for Tharga.Test per the explicit "integrated pattern from day one" guidance (line 1878)

All three are bundled in one PR, mirroring the `feature/icon-and-docs` branch convention already used by Tharga.Mcp and Tharga.Runtime.

## Goal
- Replace the WordPress-hosted PackageIconUrl with the clean `thargelion.net/assets/component-test.png`.
- Stand up a DocFX-generated documentation site at `test.tharga.net`, modeled on Tharga.Mcp's `docs/` layout.
- Wire the docs build/deploy into the existing `build.yml` so it runs only after `release` succeeds — never as a separate `docs.yml`.

## Scope
**Source / package**
- `Tharga.Test.Toolkit/Tharga.Test.Toolkit.csproj`: replace `<PackageIconUrl>` with `https://thargelion.net/assets/component-test.png`

**Documentation site (`docs/`)** — Mcp pattern:
- `docs/docfx.json` (metadata source = `Tharga.Test.Toolkit.csproj`, logo = `component-test.png`, custom template `thg`)
- `docs/CNAME` = `test.tharga.net`
- `docs/index.md` — landing page (name, summary, package, quick start, repo)
- `docs/toc.yml` — Home / Articles / API
- `docs/articles/index.md` + `toc.yml`
- `docs/articles/getting-started.md`
- `docs/articles/assignment-checks.md` — `IsAssigned`, `AssignmentIssues`, `TestAssignments`
- `docs/articles/dependency-tests.md` — `DependencyTestBase`
- `docs/templates/thg/public/main.css` — logo-size override (carbon-copy of Mcp's)
- `docs/api/*.yml` — generated by `docfx metadata` and committed (Mcp pattern, helps PR review)
- `.gitignore` — exclude `docs/_site/`

**README**
- Add `**Docs:** [test.tharga.net](https://test.tharga.net)` line near the top.

**CI workflow (`.github/workflows/build.yml`)** — Runtime reference:
- Add `pages: write` and `id-token: write` to workflow `permissions`
- Append `docs` job (`needs: release`, runs `docfx docs/docfx.json`, uploads Pages artifact)
- Append `docs-deploy` job (`needs: docs`, `concurrency: pages`, `environment: github-pages`, `actions/deploy-pages@v4`)
- Do NOT add a separate `docs.yml`.

## Out of scope (manual follow-ups in GitHub / DNS UI)
- DNS: `CNAME test.tharga.net → tharga.github.io`
- Repo Settings → Pages → Source = "GitHub Actions"
- `github-pages` environment (typically auto-created on first deploy)

## Acceptance criteria
- `dotnet build -c Release` still clean (0 warnings)
- `docfx docs/docfx.json` builds locally without errors
- PR opened against `master`, workflow runs green
- After merge: stable release `1.14.9` published, docs deployed at `test.tharga.net`

## Done condition
User confirms the docs site is reachable and the three Requests.md items can be marked Done.
